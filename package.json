{
  "name": "finops-agent",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:backend": "cd packages/backend && uvicorn main:app --reload --port 8001",
    "dev:frontend": "cd packages/frontend && npm start",
    "deploy": "sst deploy",
    "deploy:prod": "sst deploy --stage prod"
  }
}
